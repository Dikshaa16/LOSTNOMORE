services:
  - type: web
    name: lostnomore
    env: python
    region: oregon
    buildCommand: "./build.sh"
    startCommand: "cd LostNoMore_flask && gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
